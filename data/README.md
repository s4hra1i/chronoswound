# Data

Raw participant-level data must not be committed to version control. Generate the synthetic demonstrator with:

```bash
chronoswound generate --output data/synthetic_wounds.csv
```

The generated values are illustrative and must not be interpreted as biological reference ranges.
